# Deliverable 3.4 — End to End Testing

**Project:** Vaquita — Gamified Savings (SCF #42) · **Tranche:** 3 (Mainnet)
**Status:** In progress
**Index:** [`README.md`](./README.md)

> **Description.** Implementation of a full testing pipeline including automated end-to-end tests, integration tests, and manual regression testing across supported browsers and wallets. Ensures all core user flows operate reliably before production release.
>
> **Completion criteria.** Automated end-to-end test suite covering onboarding, deposits, withdrawals, and leaderboard/NFT badge interactions with integration tests validating smart contract interactions with the frontend. Manual regression testing across supported wallets (Freighter, Albedo, and ≥3 additional wallets). CI pipeline report showing successful test runs with ≥95% pass rate across all critical user flows.

---

## 1. Criteria → evidence

| # | Criterion | Evidence | Status |
|---|---|---|---|
| C1 | Automated e2e suite covering onboarding, deposits, withdrawals, leaderboard and NFT badge interactions | §3 — five spec files, **14/14 passing in CI** | **Met** |
| C2 | Integration tests validating smart-contract interactions with the frontend | §4 — testnet integration suite | **Met** |
| C3 | Manual regression across Freighter, Albedo and ≥3 further wallets | §5 — protocol published, run under way | In progress |
| C4 | CI pipeline report, ≥95% pass rate on critical flows | §6 — e2e at 100 %, contracts at 100 % | In progress |

## 2. Test pyramid

The full strategy is [`../testing.md`](../testing.md); the requirement → artifact map is
[`../qa/e2e-coverage-map.md`](../qa/e2e-coverage-map.md). Each layer only tests what the
layer below cannot: contract arithmetic is not re-tested in e2e, and wallet popups are not
tested in Playwright.

| Layer | What it proves | Network | Scale |
|---|---|---|---|
| Rust contract tests | Deposit/withdraw accounting, early-withdraw fee, randomised solvency invariant, soulbound mint with backend signature, pause and timelocked upgrade, one regression per security finding | in-process Soroban env | 169 tests, ≥ 80 % line coverage enforced |
| Vitest unit (web) | Soroban tx builders, deposit-id computation, vault/Blend query parsing, tx-error humanisation, badge error mapping, feature flags | none | 101 tests |
| Vitest unit (api + shared) | Badge and leaderboard services, config readiness, shared helpers | none | 132 tests |
| Integration | The frontend's contract clients against the deployed testnet contracts | testnet | 10 tests |
| Playwright e2e | User flows through the real UI, wallet signing stubbed at the Pollar adapter | testnet | 14 tests |
| Manual wallet regression | Real wallet UIs, browsers, reject / wrong-network paths, session restore | testnet | see §5 |

## 3. Automated end-to-end suite (C1)

`apps/web/e2e/`, run with `pnpm --filter @vaquita/web test:e2e`. Each flow named in the
criteria maps to one spec file:

| Flow named in the criteria | Spec |
|---|---|
| Onboarding | `apps/web/e2e/onboarding.spec.ts` |
| Deposits | `apps/web/e2e/deposit.spec.ts` |
| Withdrawals (matured + early) | `apps/web/e2e/withdraw.spec.ts` |
| Leaderboard interactions | `apps/web/e2e/leaderboard.spec.ts` |
| NFT badge interactions (claim + mint) | `apps/web/e2e/badges.spec.ts` |

Wallet signing is stubbed at the Pollar adapter boundary, so no extension is needed and the
suite is deterministic in CI. Real wallet UIs are covered by the manual matrix in §5.

**Workflow:** `.github/workflows/e2e.yml` — PRs touching `apps/web/**`, a nightly schedule,
and manual dispatch. Artifacts: `playwright-report` (HTML + traces on failure) and
`e2e-results` (JUnit + JSON).

## 4. Integration tests (C2)

`apps/web/src/networks/stellar/__integration__/`, run with
`pnpm --filter @vaquita/web test:integration` against the **deployed testnet contracts**:
`compute_deposit_id`, `deposit`, `withdraw`, `get_position`, `mint_badge` with a claim
signed by the API, and the SEP-10-style session against the API.

Contract addresses are read from `GET /api/v1/config`, never hardcoded, because testnet
resets move the Blend and DeFindex addresses. Secrets come from the environment.

**Workflow:** `.github/workflows/integration-tests.yml` — PRs touching
`apps/web/src/networks/**` or `contracts/**`, plus manual dispatch. Artifact
`integration-junit`.

## 5. Manual wallet regression (C3)

**Protocol:** [`../qa/wallet-regression-matrix.md`](../qa/wallet-regression-matrix.md) —
scope, wallet list, case list (`W-01` … `W-13`), run header template, results matrix and
tally rules.

**Sign-off set:** Freighter, Albedo, xBull, Rabet, Hana — satisfying "Freighter, Albedo,
and ≥3 additional wallets". Lobstr is mainnet-only and optional. One social-login column is
kept as the control for the custodial branch.

Run [`2026-08-25-38cdf27`](../qa/evidence/2026-08-25-38cdf27/results.md) covers Freighter on
Chrome against testnet, with connect and session restore passing and a locked 5 USDC
deposit confirmed by two on-chain transactions.

## 6. CI pipeline report (C4)

| Workflow | Run | Passed / total | Rate |
|---|---|---|---|
| `e2e.yml` — Web E2E (chromium) | [33070784847](https://github.com/vaquita-labs/stellar/actions/runs/33070784847) · `main` @ `c40a911` | 14 / 14 | **100 %** |
| `contracts-ci.yml` | push `main`/`dev`, PR on `contracts/**` | 169 / 169 | **100 %** |

Reporting workflows and their artifacts:

| Workflow | Trigger | Reports / artifacts |
|---|---|---|
| `.github/workflows/e2e.yml` | PR on `apps/web/**`, nightly, manual | `playwright-report`, `e2e-results`; Step Summary passed/total |
| `.github/workflows/integration-tests.yml` | PR on `apps/web/src/networks/**`, `contracts/**`, manual | `integration-junit`; Step Summary passed/total |
| `.github/workflows/contracts-ci.yml` | push `main`/`dev`, PR on `contracts/**` | `contracts-lcov`, `contracts-coverage-html`; Codecov (flag `contracts`, 80 % threshold) |
| `.github/workflows/web-ci.yml` | PR on `apps/web/**`, `packages/**` | `web-vitest-junit`; per-package pass/fail table on the job summary |
| `.github/workflows/api-ci.yml` | PR on `apps/api/**`, `packages/**` | `api-vitest-junit`; per-package pass/fail table on the job summary |

**How the rate is computed** ([`../testing.md`](../testing.md) §3): automated sources use
`passed ÷ total` from the workflow Step Summaries on the submitted commit, a flaky retry
counting once by final status; the manual matrix uses `PASS ÷ (PASS + FAIL)` with `N/A`
excluded. Contract and unit suites must be 100 % green on the same commit and do not enter
the rate.

Coverage trend: https://app.codecov.io/gh/vaquita-labs/stellar
