// Dimensiones base del mapa. Todo el mundo 3D (tiles, edificios, vaquita,
// grilla de edición) se construye a partir de estas tres constantes.
export const MAP_SIZE = 14;
export const TILE_SIZE = 1;
export const TILE_HEIGHT = 1;
