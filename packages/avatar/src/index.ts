/**
 * @vaquita/avatar — the single source of truth for Vaquita's character avatars.
 *
 * Zero runtime dependencies, no DOM and no React, so the API can validate a
 * config with the exact same catalog the web app renders it with.
 */
export * from './types';
export * from './palettes';
export * from './catalog';
export * from './config';
export * from './compose';
